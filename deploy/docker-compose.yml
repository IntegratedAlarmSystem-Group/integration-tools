version: "2"

services:
  kafka:
    image: spotify/kafka:latest

  cdb:
    image: cdb:latest

  alma-weather-plugin:
    image: alma-weather-plugin:latest
    links:
      - ${KAFKA_SERVER}

  ias-converter:
    image: ias:latest
    links:
      - ${KAFKA_SERVER}
    volumes_from:
      - cdb
    command: "-l s -Dorg.eso.ias.converter.kafka.servers=${KAFKA_SERVER}:9092 org.eso.ias.converter.Converter ConverterID -jcdb /usr/src/cdb/"

  ias-supervisor:
    image: ias:latest
    links:
      - ${KAFKA_SERVER}
    volumes_from:
      - cdb
    command: "-l s -Dorg.eso.ias.bootstrap.servers=${KAFKA_SERVER}:9092 org.eso.ias.supervisor.Supervisor SupervisorID -jcdb /usr/src/cdb/"

  ias-sender:
    image: ias:latest
    links:
      - ${KAFKA_SERVER}
    volumes_from:
      - cdb
    command: "-l s -Dorg.eso.ias.senders.kafka.servers=${KAFKA_SERVER}:9092 -Dorg.eso.ias.senders.webserver.uri=ws://webserver/core/ org.eso.ias.webserversender.WebServerSender WebServerSenderID"

  webserver:
    image: ias-webserver:latest

  display:
    image: ias-display:latest

  nginx:
    build: ./nginx
    volumes_from:
     - display
    ports:
      - "80:80"
