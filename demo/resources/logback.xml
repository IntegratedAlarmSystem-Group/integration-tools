<configuration packagingData="false">

    <timestamp key="tStamp" datePattern="yyyy-MM-dd-HH:mm:ss"/>

    <shutdownHook/>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %level %file - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <append>true</append>
        <prudent>true</prudent> <!--  Can slow down the logging -->
        <file>log/${log_file_name}.${tStamp}.log</file>

        <encoder>
            <pattern>%d{yyyy-MM-dd-HH:mm:ss} %level %file - %msg%n</pattern>
            <outputPatternAsHeader>true</outputPatternAsHeader>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
